/**
 * Entry point for the Vaidyah Clinical Service.
 *
 * Importing this module bootstraps the Express application,
 * connects to PostgreSQL, and starts listening on the configured port.
 */

import app from './app';

export default app;
