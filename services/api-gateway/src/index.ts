/**
 * Entry point for the Vaidyah API Gateway.
 *
 * Importing this module bootstraps the Express application,
 * connects to Redis and PostgreSQL, and starts listening.
 */

export { app, server } from './app';
