###############################################################################
# Vaidyah Platform - Voice Service
# Internal service for voice/audio processing (Python/FastAPI, port 8001)
# Handles speech-to-text, text-to-speech, audio streaming, and transcription.
# Higher memory allocation for audio processing workloads.
###############################################################################
apiVersion: apps/v1
kind: Deployment
metadata:
  name: voice-service
  namespace: vaidyah
  labels:
    app.kubernetes.io/name: voice-service
    app.kubernetes.io/part-of: vaidyah
    app.kubernetes.io/component: ai
    app.kubernetes.io/version: "1.0.0"
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: voice-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: voice-service
        app.kubernetes.io/part-of: vaidyah
        app.kubernetes.io/component: ai
        vaidyah.health/tier: internal
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8001"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: vaidyah-service-account
      terminationGracePeriodSeconds: 45
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - voice-service
                topologyKey: kubernetes.io/hostname
            - weight: 50
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - voice-service
                topologyKey: topology.kubernetes.io/zone
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: voice-service
      containers:
        - name: voice-service
          image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/vaidyah/voice-service:${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8001
              protocol: TCP
          envFrom:
            - configMapRef:
                name: vaidyah-config
            - secretRef:
                name: vaidyah-secrets
          env:
            - name: SERVICE_NAME
              value: voice-service
            - name: PORT
              value: "8001"
            - name: UVICORN_WORKERS
              value: "2"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 18
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: cache
              mountPath: /home/appuser/.cache
            - name: audio-scratch
              mountPath: /var/lib/voice/scratch
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 200Mi
        - name: cache
          emptyDir:
            sizeLimit: 500Mi
        - name: audio-scratch
          emptyDir:
            sizeLimit: 500Mi
---
###############################################################################
# Service
###############################################################################
apiVersion: v1
kind: Service
metadata:
  name: voice-service
  namespace: vaidyah
  labels:
    app.kubernetes.io/name: voice-service
    app.kubernetes.io/part-of: vaidyah
    app.kubernetes.io/component: ai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: voice-service
  ports:
    - name: http
      port: 8001
      targetPort: http
      protocol: TCP
---
###############################################################################
# Horizontal Pod Autoscaler
# Higher max replicas (8) to handle concurrent voice/audio sessions.
###############################################################################
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: voice-service-hpa
  namespace: vaidyah
  labels:
    app.kubernetes.io/name: voice-service
    app.kubernetes.io/part-of: vaidyah
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: voice-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 30
      policies:
        - type: Pods
          value: 2
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Pods
          value: 1
          periodSeconds: 120
---
###############################################################################
# Pod Disruption Budget
###############################################################################
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: voice-service-pdb
  namespace: vaidyah
  labels:
    app.kubernetes.io/name: voice-service
    app.kubernetes.io/part-of: vaidyah
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: voice-service
